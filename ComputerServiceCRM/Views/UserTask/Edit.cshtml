@model Data.Models.TaskData

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.ID)

    <div class="form-horizontal">
        <h4>TaskData</h4>
        <hr />
        <div class="form-group">
            <label class="control-label col-md-2">Not</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.CompletingNote, new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Tamamla" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Listeye dön", "List")
</div>
